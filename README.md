# INFOTECS_test_task

### Build: make all
 // creates test application ./app

#### Usage: LD_LIBRARY_PATH+=. ./app file level(info=default|warning|critical)
// LD_var for temporary .so placement 

**Supports only .txt files**